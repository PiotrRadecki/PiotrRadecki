<Window x:Class="Serializacja.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Serializacja"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Column="1" Text="{Binding Imie, Mode=TwoWay}"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Nazwisko, Mode=TwoWay}"></TextBox>
        <Button Content="Ok" Grid.Row="2" Click="Button_Click" />
        <Button Content="Cancel" Grid.Row="2" Grid.Column="1" Click="Button_Click_1" />
    </Grid>
</Window>
